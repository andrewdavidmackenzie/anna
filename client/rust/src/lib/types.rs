use std::time::SystemTime;

/// An anna Address
pub type Address = String;
/// An anna Key
pub type Key = String;
/// An anna Timepoint
pub type TimePoint = SystemTime;
/// The ID of a Thread
pub type ThreadID = usize;
